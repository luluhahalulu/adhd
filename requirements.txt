# 核心计算与数据处理 (您列出的版本是正确的)
numpy==1.26.4
pandas==2.2.2
matplotlib==3.8.0

# 机器学习核心 (您列出的版本是正确的)
scikit-learn==1.5.1
xgboost==2.0.3
shap==0.46.0

# --- 必须补充的库 (您的代码中使用了这些，但之前的列表中没有) ---
# 1. 必须：用于处理不平衡数据的 SMOTE 和 Pipeline
imbalanced-learn==0.12.3

# 2. 必须：用于代码中的统计检验 (scipy.stats)
scipy==1.13.1

# 3. 必须：用于代码中的绘图 (sns.heatmap 等)
seaborn==0.13.2

# 4. 可选：代码中导入了 lightgbm，虽然最终选了随机森林，但为了不报错建议安装
lightgbm==4.3.0

